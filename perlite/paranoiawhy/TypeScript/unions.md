# union types

```typescript
type bool = true | false;
```

# typeof

```javascript
// string / number / boolean / undefined / function
typeof s === "string";
typeof n === "number";
typeof b === "boolean";
typeof undefined === "undefined";
typeof f === "function";

// array
Array.isArray(a);
```